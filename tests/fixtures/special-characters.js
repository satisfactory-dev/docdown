/**
 * Logs markdown text to console
 * 
 * @param {string} value such as `"\\[bar\\]\\(baz\\)"`, "*", "[", "]", "\*", "\[", or "\]"
 * 
 * @returns {void}
 * 
 * @example
 * 
 * `foo("\\[bar\\]\\(baz\\)");`
 */
function foo(value) {
  console.log(value);
}